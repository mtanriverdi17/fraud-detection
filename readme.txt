To run the project you need to run this command: ' pip install -r requirements.txt'
After it will install the required packages

Then you need to run this command to open the web app: ' streamlit run detection_page.py ' 

And the notebook for models is included.